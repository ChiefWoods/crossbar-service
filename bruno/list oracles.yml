info:
  name: list oracles
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/oracles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
