info:
  name: list stream endpoint
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/stream/ws"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
