info:
  name: list gateways
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/gateways?network=mainnet"
  params:
    - name: network
      value: mainnet
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
