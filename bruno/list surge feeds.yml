info:
  name: list surge feeds
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/stream/surge_feeds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
